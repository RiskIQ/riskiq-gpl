apply plugin: "java"

version='1.1.0'

repositories {
    maven {
        url "https://plugins.gradle.org/m2/"
    }
}
dependencies {
    testCompile(group: 'junit', name: 'junit', version: '4.13.1')
}